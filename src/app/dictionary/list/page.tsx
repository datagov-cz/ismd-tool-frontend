import { ISMDListWrapper } from '@/components/ontologyList/ISMDlistWrapper';

const ISMDList = async () => {
  return (
    <div className="w-full bg-primary-subtlest">
      <ISMDListWrapper />
    </div>
  );
};

export default ISMDList;
